git commit -m
git add all
git add remote <duong dan toi git>
git push -u origin main
//xem trang thai
git status
//xem lich su
git log
//sau khi clone về tạo nhánh mới để pull request bằng lệnh
git checkout -b <tên nhánh>
//tại nhánh này thực hiện add and commit như cũ. 
//push bằng lệnh
git push origin <tên nhánh vừa tạo>
//sau khi push sẽ có một URL tại remote. copy lên brower. rồi xác nhận pull request
